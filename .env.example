DATABASE_URL=postgresql://erp_app:erp_app@localhost:5432/erp_test
MIGRATIONS_DATABASE_URL=postgresql://erp_test:erp_test@localhost:5432/erp_test


NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Optional: only needed for the AI-assisted CSV column mapping suggestion
# (lib/ai/csv-mapping.ts). The import flow itself works fully manually
# without this key — AI only pre-fills the mapping review screen.
OPENAI_API_KEY=